name: offline_with_connection_package
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  bloc: "8.1.1"
  dio: "5.1.2"
  flutter:
    sdk: flutter
  flutter_bloc: "8.1.2"

dev_dependencies:
  bloc_test: "9.1.1"
  flutter_lints: "2.0.1"
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
